# @ybouane/liquidglass ships a complete dist bundle but its 1.0.3 postinstall
# references patch-package without declaring it. No project dependency needs
# lifecycle compilation, so installs remain deterministic with scripts disabled.
ignore-scripts=true
